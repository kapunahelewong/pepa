---
title: Authentication
description: How API keys and auth headers work.
nav: API Reference
order: 1
---

# Authentication

Placeholder page for where OpenAPI-driven reference content will live.
The plan: embed a renderer like Scalar's API reference component here rather
than hand-building a request-builder/schema-explorer — see the README for
why that piece specifically isn't worth building from scratch.

## Example request

```bash
curl https://api.example.com/v1/widgets \
  -H "Authorization: Bearer YOUR_API_KEY"
```
