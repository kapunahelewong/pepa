StylesPath = .vale/styles

# Vale downloads rule packages into StylesPath via `vale sync`.
# Run once after cloning: brew install vale && pnpm vale:sync
Packages = Google

MinAlertLevel = suggestion

# Apply Google style to all Markdown and MDX files.
[*.{md,mdx}]
BasedOnStyles = Google
