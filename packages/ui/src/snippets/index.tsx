/**
 * Reusable content snippets — edit here, updates everywhere.
 *
 * Each export is a React component so it can contain anything:
 * a word, a sentence, a paragraph, a code block, or a whole section.
 *
 * Usage in any MDX file:
 *   import { AuthNote, BearerExample } from "docs-ui/snippets"
 *   <AuthNote />
 *   <BearerExample />
 *
 * Convention: keep snippets small and focused. If a snippet grows
 * into a full page, it should become a real doc instead.
 */

export { AuthNote } from "./AuthNote";
export { BearerExample } from "./BearerExample";
